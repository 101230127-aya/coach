class StudentModel {
  final int id;
  final String name;
  final int strength; // for later filtering

  StudentModel({
    required this.id,
    required this.name,
    required this.strength,
  });
}
